import Popup from '@/UI/containers/Popup';
import styles from './styles.module.scss';

const ProductPopup = ({
  isProductActive,
}) => {
  return (
    <Popup isOpen={isProductActive}>
      Bebebebebe
    </Popup>
  );
};

export default ProductPopup;